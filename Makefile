#----------------------------------------------------------------------
# CS415/515 Assignment 3
#----------------------------------------------------------------------

CC = gcc
CHPL = chpl

%: %.chpl
	$(CHPL) -o $@ $<

%: %.c
	$(CC) -o $@ $< -g -lm


